apiVersion: v1
kind: Service
metadata:
  name: {{ include "btmt-app.fullname" . }}
  labels:
    {{- include "btmt-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "btmt-app.selectorLabels" . | nindent 4 }}
